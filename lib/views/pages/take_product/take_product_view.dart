import 'package:flutter/material.dart';

class TakeProductPage extends StatelessWidget {
  const TakeProductPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("TakeProductPage"),
      ),
    );
  }
}